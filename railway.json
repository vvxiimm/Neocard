{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd apps/api && prisma generate && cd ../.. && pnpm --filter @nexus/api build",
    "watchPatterns": [
      "apps/api/**",
      "packages/**"
    ]
  },
  "deploy": {
    "startCommand": "cd apps/api && pnpm db:push && node dist/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
